import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  // <React.StrictMode>
    <App />
  // </React.StrictMode>
  // git add . && git commit -m "Note changes will be uploaded to API" && git push
  // git add . && git commit -m "Note changes will be uploaded to the database" && git push
)
